ifeq ($(FEATURE_LOG),y)
FEATURE_CFLAGS += -DFEATURE_LOG
FEATURE_LDFLAGS += -L$(MODULE_ROOT)/Log/ -lmyLog
endif

ifeq ($(FEATURE_MSG),y)
FEATURE_CFLAGS += -DFEATURE_MSG
FEATURE_LDFLAGS += -L$(MODULE_ROOT)/Msg/ -lmyMsg
endif

ifeq ($(FEATURE_TPOOL),y)
FEATURE_CFLAGS += -DFEATURE_TPOOL
FEATURE_LDFLAGS += -L$(MODULE_ROOT)/TPool/ -lmyTPool
endif

ifeq ($(FEATURE_MHEALTH),y)
FEATURE_CFLAGS += -DFEATURE_MHEALTH
FEATURE_LDFLAGS += -L$(MODULE_ROOT)/Health/ -lmyModuleHealth
endif
